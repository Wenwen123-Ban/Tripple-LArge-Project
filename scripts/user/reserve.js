async function reserveBook(bookId){const studentId=window.currentStudentId||sessionStorage.getItem('student_id')||''; const res=await fetch('/api/transactions/reserve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({book_id:bookId,student_id:studentId})}); const data=await res.json(); alert(data.status==='reserved'?'Book reserved.':(data.error||'Failed')); if(data.status==='reserved'&&window.loadBooks)window.loadBooks(true)}
