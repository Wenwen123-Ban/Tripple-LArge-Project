async function loadNotifs(){const f=document.getElementById('notif-filter').value;const r=await fetch('/api/user/notifications?filter='+f);const d=await r.json();document.getElementById('notif-count').textContent=`${d.unread||0}/${d.total||0}`;document.getElementById('notif-page-list').innerHTML=(d.items||[]).map(n=>`<div class='notif-item ${n.is_read?'':'unread'}'><div>${n.title}</div><small>${n.created_at||''}</small></div>`).join('')||'<div class="notif-empty">No notifications.</div>'}
document.addEventListener('DOMContentLoaded',()=>{document.getElementById('notif-filter').addEventListener('change',loadNotifs);document.getElementById('notif-clear').addEventListener('click',async()=>{await fetch('/api/user/notifications/clear',{method:'POST'});loadNotifs()});loadNotifs()});
